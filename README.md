# Captain Blocks

WordPress plugin containing custom Gutenberg blocks built for the [Captain block theme](https://github.com/baillieogrady/captain-block-theme)

1. Run `npm install`
3. Run `npm run watch` for dev 
4. Run `npm run build` to build production assets